// media-adapter.js
//
// PLATZHALTER — bewusst ohne Funktion in Schritt 1.
// Soll ab docs/ROADMAP.md, Phase 9 ("YouTube, MP4 und HLS") eine
// einheitliche Schnittstelle bereitstellen, damit player-controller.js
// unabhängig von der jeweiligen Videoquelle (lokales MP4, YouTube-
// Einbettung, HLS-Stream) funktioniert.
//
// In Schritt 1 ausdrücklich nicht vorhanden.

export function createMediaAdapter() {
  throw new Error(
    '[ONLANG TV] media-adapter ist in Schritt 1 noch nicht implementiert. ' +
    'Siehe docs/ROADMAP.md, Phase 9.'
  );
}
